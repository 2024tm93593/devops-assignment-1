apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app
  namespace: canary
spec:
  type: ClusterIP
  selector:
    app: aceest-fitness-app
    track: canary
  ports:
    - port: 5000
      targetPort: 5000
