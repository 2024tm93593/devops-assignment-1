apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app
  namespace: stable
spec:
  type: ClusterIP
  selector:
    app: aceest-fitness-app
    track: stable
  ports:
    - port: 5000
      targetPort: 5000
