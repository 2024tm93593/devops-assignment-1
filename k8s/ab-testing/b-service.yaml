apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app
  namespace: b-site
spec:
  type: ClusterIP
  selector:
    app: aceest-fitness-app
    variant: b
  ports:
    - port: 5000
      targetPort: 5000
