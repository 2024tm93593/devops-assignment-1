apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app
  namespace: a-site
spec:
  type: ClusterIP
  selector:
    app: aceest-fitness-app
    variant: a
  ports:
    - port: 5000
      targetPort: 5000
