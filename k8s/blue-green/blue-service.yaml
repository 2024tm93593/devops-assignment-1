apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app
  namespace: blue
spec:
  type: ClusterIP
  selector:
    app: aceest-fitness-app
    version: blue
  ports:
    - port: 5000
      targetPort: 5000
