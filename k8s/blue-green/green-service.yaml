apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app
  namespace: green
spec:
  type: ClusterIP
  selector:
    app: aceest-fitness-app
    version: green
  ports:
    - port: 5000
      targetPort: 5000
