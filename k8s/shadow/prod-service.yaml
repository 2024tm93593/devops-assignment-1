apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-app
  namespace: prod
spec:
  type: ClusterIP
  selector:
    app: aceest-fitness-app
    track: prod
  ports:
    - port: 5000
      targetPort: 5000
